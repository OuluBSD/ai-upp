AGENTS

Scope
- Applies to `upptst/DotnetApp`.

Purpose
- Minimal DOTNET builder smoke test package.

Conventions
- Keep the app simple and console-based.
- Only include `DotnetApp.cs` in the package file list.
