# Task: Plan Management
# Status: TODO

## Objective
Implement the `plan` subcommand to list, show, and manage plans using `PlanParser`.

## Requirements
- Link `MaestroCLI` with `Maestro` package (already done in UPP).
- Use `PlanParser` to load plans from `uppsrc/AI/plan`.
- Implement `plan list`: Show all plans and their status.
- Implement `plan show <name>`: Show details of a specific plan.
- Implement basic `plan add` (optional/initial version).
