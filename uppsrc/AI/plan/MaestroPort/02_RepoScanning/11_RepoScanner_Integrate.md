# Task: RepoScanner Integrate
# Status: DONE

## Objective
Use `UppParser` inside `RepoScanner` to fill detailed `PackageInfo`.

## Requirements
- When `.upp` is found, parse it.
- Convert parsed data to `PackageInfo` fields (dependencies, groups).
