# Task 0401 - Model Loading in Executor

## Goal
Load and render 3D models in the standalone executor.

## Scope
- Executor uses compiled project data to load models.
- Support both full export and lightweight export paths.
- Ensure render path works for static meshes and materials.

## Success Criteria
- ModelMediaShell renders imported models from compiled projects.
- Paths resolve via ShareDirFile-based project roots.

## Status
- Done
