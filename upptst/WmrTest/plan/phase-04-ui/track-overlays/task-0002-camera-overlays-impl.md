Task 0002 - Camera overlay implementation

Checklist
- Expose overlay data from SoftHMD StereoTracker (keypoints + matches).
- Cache overlay in WmrTest and render it on camera view.
- Add View menu toggles for descriptors, IDs, match lines, stats, split view.

Deliverables
- Debug overlays drawn over bright/dark stereo frames.
- Menu-driven toggles wired to CameraCtrl.
