# Task: Project Skeleton & Maestro Integration
# Status: TODO

## Objective
Ensure MaestroHub is correctly set up to use the new C++ Maestro package and establishes the basic GUI application structure.

## Requirements
- Update `MaestroHub.upp` to include `Maestro` package.
- Verify `MaestroHub.h` and `main.cpp` initialize the `Maestro` engine components (e.g., `RegisterMaestroTools`).
- Implement a basic `TopWindow` with `AIChatCtrl` as the central widget.
- Ensure the application builds and runs, connecting to the configured AI engine (Gemini).
