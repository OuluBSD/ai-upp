AGENTS

Scope
- Applies to `uppsrc/Draw/Media`.

Purpose
- Headless media core types (audio/video frames, streams, file input helpers).
- Shared by api/Media and other headless systems.

Guidelines
- No CtrlLib or UI code.
- Keep interfaces explicit and debuggable.
