# Task: Configuration UI
# Status: DONE

## Objective
Visual interface for `maestro settings`.

## Requirements
- **Property Grid**: Use `PropertyGrid` (if available) or `ArrayCtrl` to edit key-value pairs.
- **Profile Manager**: UI to list, save, load settings profiles.
- **Search**: Filter settings by key.
