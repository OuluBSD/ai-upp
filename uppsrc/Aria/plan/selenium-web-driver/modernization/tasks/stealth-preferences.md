# Task: Implement Firefox Stealth Preferences

## Description
Add a comprehensive set of Firefox preferences to `Capabilities` or a new `FirefoxOptions` class to mask the automated nature of the browser.

## Steps
1. Define a list of stealth preferences based on `driver.py`.
2. Update `Capabilities` or `Firefox` class to support setting these preferences.
3. Ensure they are correctly serialized to JSON for the WebDriver protocol.

## Status
Completed
