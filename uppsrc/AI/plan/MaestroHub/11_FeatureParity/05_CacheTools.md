# Task: Cache & Maintenance Tools Integration

# Status: TODO

# Description
Implement GUI tools for managing internal AI caches and persistent task state tracking.

# Objectives
- Add "Maintenance" tab to the Fleet Dashboard.
- Support inspecting/clearing AI response cache (`cache`).
- Support inspecting/clearing persistent track cache (`track-cache`).
- Add "Sync Core" tool for resolving discrepancies between core and track cache.

# UI Requirements
- High-density lists for cache entries.
- "Purge All" button with standard confirmation dialog.
- JSON view for inspecting cached AI responses.
