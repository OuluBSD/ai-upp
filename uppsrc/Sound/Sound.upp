flags(WIN32 (CLANG | GCC)) SYS_PORTAUDIO;

uses(!SYS_PORTAUDIO) plugin/portaudio;

library(SYS_PORTAUDIO !(WIN32 !MSC)) portaudio;

library(SYS_PORTAUDIO WIN32 !MSC) libportaudio.dll.a;

file
	AGENTS.md,
	Types.h,
	SoundSystem.h,
	Sound.cpp,
	SoundStream.cpp,
	SoundSystem.cpp,
	SoundDaemon.h,
	SoundDaemon.cpp,
	SoundThread.h,
	SoundThread.cpp,
	SoundThread.inl highlight cpp,
	Discussion.h,
	Discussion.cpp,
	SoundClip.h,
	Sound.h;

