# ScriptTranspiler

Python-to-JavaScript transpilation support for browser-hosted `.gamestate` execution.

## Rules

- Keep authored source Python-first.
- Target a documented subset rather than promising full Python compatibility.
- Prefer deterministic, explicit failures for unsupported constructs.
- Keep this package non-GUI.
