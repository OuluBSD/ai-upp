# Task: AssemblyInfo Struct
# Status: TODO

## Objective
Define `AssemblyInfo` in `uppsrc/Maestro/AssemblyInfo.h`.

## Requirements
- Fields: `name`, `dir`, `packages`, `package_dirs`.
- `Jsonize` support.
