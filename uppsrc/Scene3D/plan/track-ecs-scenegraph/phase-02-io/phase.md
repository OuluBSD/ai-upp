# Phase 02 - IO & Project Format

## Goal
Load/save Scene3D projects directly from ECS (no Geom* storage).

## Exit Criteria
- Scene3D load creates ECS entities/components.
- Scene3D save serializes ECS state.
- Execution manifest references ECS paths.
