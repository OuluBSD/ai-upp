AGENTS

Scope
- Applies to `upptst/UwpDemoRoom`.

Purpose
- UWP test harness for the DemoRoom ECS scene.

Notes
- Uses UWP + DX12 flags; entrypoint should launch the DemoRoom `AppView` runtime.
- Ensure DemoRoom assets are packaged into the AppX and accessible via `ms-appx:///`.
