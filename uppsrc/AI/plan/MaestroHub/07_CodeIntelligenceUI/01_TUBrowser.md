# Task: TU Browser
# Status: DONE

## Objective
Visual interface for `maestro tu` commands.

## Requirements
- **Package List**: List all packages with TU status.
- **Symbol Search**: Search bar to querying symbols (`tu query`).
- **Dependency Graph**: Visualization of package dependencies.
- **Build Action**: Button to trigger `tu build` for selected package.
