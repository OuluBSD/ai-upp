# AmpTemplateGuiProbe

Minimal GUI-host probe for native C++ AMP. It exists only to compare
`GUI_APP_MAIN` with the console AMP probe and must remain observable through
`COUTLOG`, without modal UI behavior.
