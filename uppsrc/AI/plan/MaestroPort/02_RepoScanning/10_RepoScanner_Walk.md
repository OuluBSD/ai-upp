# Task: RepoScanner Walk
# Status: DONE

## Objective
Implement `RepoScanner::Scan` in `RepoScanner.cpp`.

## Requirements
- Recursive directory traversal.
- Detect `.upp` files and `CMakeLists.txt`.
- Populate `PackageInfo` basic fields.
