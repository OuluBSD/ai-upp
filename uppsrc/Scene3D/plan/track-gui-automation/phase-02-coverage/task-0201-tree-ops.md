# task-0201-tree-ops

## Goal
Cover scene tree operations (create nodes, expand/collapse, visibility/lock/RW toggles, context menus).

## Acceptance
- Tests create objects via context menu.
- Tests toggle visibility/lock/read/write and verify UI state changes.
- Expand/collapse and selection switching are validated.
