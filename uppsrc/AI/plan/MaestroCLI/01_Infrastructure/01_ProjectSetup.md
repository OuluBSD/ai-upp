# Task: MaestroCLI Project Setup
# Status: DONE

## Objective
Create the `uppsrc/MaestroCLI` package and implement a basic skeleton that handles `--help` and versioning.

## Requirements
- Create `uppsrc/MaestroCLI/MaestroCLI.upp`.
- Create `uppsrc/MaestroCLI/main.cpp`.
- Implement `Upp::CommandLine` parsing for top-level flags (`-v`, `-q`, `-s`).
- Support `help` and `--version` commands.
- Ensure it links against `Maestro` package.
