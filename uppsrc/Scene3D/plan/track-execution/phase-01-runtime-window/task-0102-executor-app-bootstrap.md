# Task 0102 - Executor App Bootstrap

## Goal
Use examples/ModelMediaShell as the standalone executor for compiled projects.

## Scope
- Load compiled project files (ByteVM-only runtime).
- Accept command line for project path in share/.
- Support execution-only loop and clean shutdown.

## Success Criteria
- ModelMediaShell opens a compiled project and runs it.
- Executor runs without ModelerApp dependencies.
- Executor can load both full and lightweight exports.

## Status
- Done
