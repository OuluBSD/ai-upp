# Task: Bind `.form` DOM Layout To Transpiled JS

## Goal

Connect the browser DOM renderer and the transpiled JavaScript runtime through a stable host API that exposes named zones, labels, buttons, and later sprites.
