# VB6 .cls and .bas Support

Implement support for VB6 class files (`.cls`) and module files (`.bas`).
Handle structure, scope (public/private), and basic definitions.
Include support for `.csi`, `.exp`, `.lvw` metadata files.
