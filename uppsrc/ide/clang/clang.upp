description "\377B";

uses
	ide/Core;

library(WIN32 MSC) libclang.lib;

library(WIN32 !MSC MSYS) "clang libclang.dll.a";

library(LCLANG !MSC) clang;

library(WIN32 !MSC !MSYS) libclang;

file
	AGENTS.md,
	clang.h,
	libclang.h,
	clang.dli,
	libclang.cpp,
	util.cpp,
	macros.cpp,
	clang.cpp,
	Visitor.cpp,
	CurrentFile.cpp,
	Indexer.cpp,
	Info readonly separator,
	Copying;

