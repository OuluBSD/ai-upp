# Task: Assembly Detection
# Status: TODO

## Objective
Implement `DetectAssemblies(const Array<PackageInfo>&)` in `RepoScanner`.

## Requirements
- Group packages by parent directory.
- Create `AssemblyInfo` objects.
