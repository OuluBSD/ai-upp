AGENTS

Scope
- Applies to `upptst/IdeCoreWorkspaceSmoke`.

Purpose
- Compile-only smoke test for `IdeCoreWorkspace`.

Notes
- Keep this package headless.
- Depend only on `ide/Core` unless the test case explicitly needs more.
