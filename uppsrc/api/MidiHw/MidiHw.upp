description "Midi Hardware API\377B0,128,0";

	uses MidiFile;
	uses(BUILTIN_PORTMIDI) plugin/portmidi;

acceptflags
	MIDI;
	uses MidiFile;
	uses(BUILTIN_PORTMIDI) plugin/portmidi;
	library(PORTMIDI) "portmidi";
	library(PORTMIDI) "portmidi";

	library(PORTMIDI) "portmidi";

file
	MidiHw.h,
	Portmidi.cpp,
	IfaceFuncs.inl highlight cpp;

mainconfig
        "" = "";
