# Task: UppParser Header
# Status: TODO

## Objective
Create `uppsrc/Maestro/UppParser.h`.

## Requirements
- Class `UppParser`.
- State members: `description`, `uses`, `files`, `mainconfigs`, `acceptflags`, `libraries`, `static_libraries`, `links`.
- Method `Parse(const String& content)`.
