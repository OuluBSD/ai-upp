# MLUI_FocusPage Package Notes

- This package is a reference design for MLUI FocusPage workflow.
- It is intentionally design-first and may not be fully production-ready.
- Goal: show how `INITBLOCK` + `Ctrl::Access(Visitor&)` can define page-like MCP views.

