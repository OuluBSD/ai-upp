# Task: UppParser Files
# Status: DONE

## Objective
Implement `ParseFiles` in `UppParser.cpp`.

## Requirements
- Parse `file "a.cpp", "b.h";`.
- Handle file blocks with options (separator, readonly, etc.).
- Populate `files` vector.
