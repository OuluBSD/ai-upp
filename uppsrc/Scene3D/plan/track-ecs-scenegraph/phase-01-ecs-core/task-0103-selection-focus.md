# Task 0103 - Selection + Focus

## Goal
Selection, focus, and active camera are driven by ECS identifiers.

## Scope
- Selection model uses entity IDs and component IDs.
- Focus camera and program camera stored in ECS.
- SceneGraph Explorer + viewport use same focus source.
