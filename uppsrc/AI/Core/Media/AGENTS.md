Purpose: Non-text content (image, audio, video) orchestrations.

- Start here when you need:
  - Media-specific pipelines and assets.

- Subpackages:
  - `Video/` (example scaffolded) → umbrella is `Video.h`.

- Public surface:
  - Use `#include <AICore2/Media/Media.h>`.
