# Task 0200 - Stage API

## Goal
Provide a Stage-style scripting API that mirrors ActionScript conventions.

## Scope
- `stage` global module with `root`, `find(path)`, `children(node)`, `create(type,name,parent)`.
- `stage.time`, `stage.frame`, `stage.fps`, `stage.goto(frame)`.
- Path resolution aligned to Scene3D tree.

## Success Criteria
- Scripts can navigate and create scene nodes via `stage`.
- Stage time/frame values are correct.

## Status
- Pending
