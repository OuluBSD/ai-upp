# Task: FileGroup Struct
# Status: DONE

## Objective
Define the `FileGroup` struct in `uppsrc/Maestro/PackageInfo.h` (create file).

## Requirements
- Fields: `String name`, `Vector<String> files`, `bool readonly`, `bool auto_generated`.
- Add `Jsonize` support.
