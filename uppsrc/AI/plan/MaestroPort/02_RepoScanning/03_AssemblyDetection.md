# Task: Assembly Detection
# Status: TODO

## Objective
Implement logic to group detected packages into "Assemblies" (logical units of related packages), mirroring `maestro/repo/assembly.py`.

## Requirements
- Struct `AssemblyInfo`.
- Function `DetectAssemblies(const Array<PackageInfo>& packages)`.
- Heuristics: Directory containment, naming conventions.

## Context
See `~/Dev/Maestro/maestro/repo/assembly.py`.
