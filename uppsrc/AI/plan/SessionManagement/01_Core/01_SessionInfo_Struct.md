# Task: SessionInfo Struct
# Status: IN_PROGRESS

## Objective
Define the `SessionInfo` struct in `uppsrc/Maestro/Engine.h`.

## Requirements
- Fields: `String id`, `String name`, `Time timestamp`, `ValueMap metadata`.
- Inherit from `Moveable<SessionInfo>`.
- Add `Jsonize` support.
