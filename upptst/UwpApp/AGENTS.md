AGENTS

Scope
- Applies to `upptst/UwpApp`.

Purpose
- Minimal UWP builder smoke test package for staging/build workflow.

Conventions
- Keep the app simple and console-based.
- Include only `UwpApp.h` from `.cpp` files.
