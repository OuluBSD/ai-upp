# Task: Basic Chat GUI
# Status: DONE

## Objective
Create a reusable `AIChatCtrl` for the chat interface and a test application `AIChat` to verify functionality.

## Requirements
- `AIChatCtrl` should handle text input and streaming output.
- Custom `MaestroItem` for rendering chat messages efficiently.
- `AIChat` app should use `TabCtrl` to prepare for multi-view layout.

## Output
- `uppsrc/Maestro/AIChatCtrl.h`
- `uppsrc/Maestro/AIChatCtrl.cpp`
- `upptst/AIChat/`
