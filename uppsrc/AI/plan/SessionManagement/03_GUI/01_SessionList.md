# Task: Session List GUI
# Status: DONE

## Objective
Implement a specialized GUI in `MaestroHub` for browsing and managing AI sessions across all backends.

## Requirements
- Replace the simple `ArrayCtrl` in `SessionSelectWindow` with a dedicated `SessionListView`.
- Support filtering by backend (Gemini, Qwen, etc.).
- Allow deleting sessions.
- Show session metadata (tokens, duration) if available.
