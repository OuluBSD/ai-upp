# Task 0202 - Execution Manifest Uses ECS

## Goal
Execution runtime reads ECS data directly (no Geom* intermediates).

## Scope
- ExecScriptRuntime entity/component addressing.
- Asset resolution from ECS.
- Runtime loading tests in Eon09 and ModelMediaShell.
