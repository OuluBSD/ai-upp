# AGENTS - reference/CommandRegistry

- This package is the generic reference console for `uppsrc/Core/CommandRegistry`.
- Keep sample commands deterministic and small.
- Register example commands in `.icpp` files with `INITBLOCK`.
- Do not add VisualStateModel-specific logic here; VSM migration belongs to a later task.
