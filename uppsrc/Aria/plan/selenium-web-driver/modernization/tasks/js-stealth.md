# Task: Implement JS Stealth Injection

## Description
Add a mechanism to automatically inject and execute stealth JavaScript snippets after a session is started or a new page is loaded.

## Steps
1. Port stealth JS snippets from `driver.py`.
2. Add a `Session` method or hook to inject these snippets.
3. Ensure they are executed early enough to avoid detection.

## Status
Completed
