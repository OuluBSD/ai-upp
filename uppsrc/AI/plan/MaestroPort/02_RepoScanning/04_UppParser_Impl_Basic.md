# Task: UppParser Basic Impl
# Status: TODO

## Objective
Create `uppsrc/Maestro/UppParser.cpp` and implement `Reset()` and basic parsing loop.

## Requirements
- `Reset()` clears all state.
- `Parse(String)` splits lines and calls specific parsers (placeholders for now).
