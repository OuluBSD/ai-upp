# VB6 .res and .frx Support

Implement binary support for VB6 resource files (`.res`).
Support for `.frx` binary form files, ensuring correct extraction of images and other binary data.
Integrate with the form parser to correctly link form controls to their binary resources.
