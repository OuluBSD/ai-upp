# Phase 06 - Compatibility Layer (Temporary)

## Goal
Keep Geom* only as temporary compatibility wrappers during migration.

## Exit Criteria
- Geom* acts as thin adapter over ECS.
- All runtime paths use ECS.
