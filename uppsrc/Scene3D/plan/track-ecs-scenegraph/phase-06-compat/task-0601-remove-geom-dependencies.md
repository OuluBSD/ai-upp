# Task 0601 - Remove Geom* Dependencies

## Goal
Eliminate code paths that still depend on Geom* data.

## Scope
- ModelerApp uses ECS only.
- Eon09/Eon runtime uses ECS only.
- Scene3D IO uses ECS only.
