# Task: Test WebDriver Package

## Description
Create unit tests for Client, Session, and Element classes to ensure they work correctly with a WebDriver server.

## Steps
1. Create a test package `upptst/WebDriverTest`
2. Implement tests for `Client::GetStatus()` and `Client::GetSessions()`
3. Implement tests for `Client::CreateSession()`
4. Implement tests for `Session` navigation and source retrieval
5. Implement tests for `Session::FindElement()` and `Element` interactions
6. Implement tests for `WaitUntil` and `WaitFor` conditions

## Dependencies
- WebDriver convention adaptation completed
- Local WebDriver server (e.g. ChromeDriver or GeckoDriver) running

## Status
Pending
